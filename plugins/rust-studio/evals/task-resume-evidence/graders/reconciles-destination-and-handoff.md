---
type: llm
weight: 3
---
Full credit requires:
1. Does not launch C or trust P's done row: the producer is not integrated on the agreed
   destination; the green worker commit is evidence for a different tree.
2. Resolves the producer/consumer None-versus-Reset contradiction against the approved
   protocol/plan before dispatch; passes the discovery and decoder-notes.md pointer to C,
   not just the incomplete task description. Does not quietly change the intended contract.
3. Reconstructs worker/source and destination worktree/branch/revision or dirty-diff identity,
   accepted criteria and command evidence, unresolved findings/repair count, and downstream
   discoveries in the durable per-task record. Missing facts are unknown, not invented.
4. Requires integration/reconciliation of the changed destination signature and refreshed
   affected acceptance/integration evidence before local P completion/unblocking C; preserves
   unrelated user edits and does not reset/reimplement P blindly.
5. Uses tasks.md as the durable local status source; distinguishes integration/verification
   from remote issue/PR state. Does not post or close anything without authorization, and
   does not claim a merge. Keeps external synchronization/merge pending explicitly.
Score 0 for dispatching C immediately, accepting worker green as destination green, destructive
reset, unauthorized tracker updates, or fabricating evidence. Otherwise missing items score 0.5.
