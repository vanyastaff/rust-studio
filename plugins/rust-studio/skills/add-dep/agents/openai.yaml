interface:
  display_name: "Add Dep"
  short_description: "Adding a Rust dependency: RUSTSEC, license, MSRV, features first"
  default_prompt: "Use $add-dep when adding a Rust dependency: RUSTSEC, license, MSRV, features first."
policy:
  allow_implicit_invocation: false
