interface:
  display_name: "Add Dep"
  short_description: "Adding a Rust dependency: check RUSTSEC, license, MSRV"
  default_prompt: "Use $add-dep when adding a Rust dependency: check RUSTSEC, license, MSRV, and features before changing Cargo.toml."
policy:
  allow_implicit_invocation: false
