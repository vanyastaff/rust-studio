interface:
  display_name: "Spec"
  short_description: "A Rust change needs a durable design decision across sessions"
  default_prompt: "Use $spec when a Rust change needs a durable design decision across sessions or tasks."
