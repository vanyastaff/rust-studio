interface:
  display_name: "Progress Bar"
  short_description: "Adding, refreshing, or removing the Claude Code status line"
  default_prompt: "Use $progress-bar when adding, refreshing, or removing the Claude Code status line."
policy:
  allow_implicit_invocation: false
