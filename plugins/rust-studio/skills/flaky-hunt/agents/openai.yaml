interface:
  display_name: "Flaky Hunt"
  short_description: "For an intermittent Rust test failure that needs reproduction"
  default_prompt: "Use $flaky-hunt for an intermittent Rust test failure that needs reproduction or containment."
