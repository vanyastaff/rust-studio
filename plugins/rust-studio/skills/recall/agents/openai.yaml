interface:
  display_name: "Recall"
  short_description: "Starting related work: recall project decisions, gotchas"
  default_prompt: "Use $recall when starting related work: recall project decisions, gotchas, conventions, and fixes; verify each holds."
