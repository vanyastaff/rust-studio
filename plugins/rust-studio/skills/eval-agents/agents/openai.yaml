interface:
  display_name: "Eval Agents"
  short_description: "Benchmarking studio reviewers in Claude Code or improving"
  default_prompt: "Use $eval-agents when benchmarking studio reviewers in Claude Code or improving instructions with a controlled training/holdout experiment."
policy:
  allow_implicit_invocation: false
