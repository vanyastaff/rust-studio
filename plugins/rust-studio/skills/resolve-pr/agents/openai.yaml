interface:
  display_name: "Resolve PR"
  short_description: "Working PR review threads, review-bot findings"
  default_prompt: "Use $resolve-pr when working PR review threads, review-bot findings, or CI failures toward merge-ready."
