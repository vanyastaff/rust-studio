interface:
  display_name: "Publish"
  short_description: "Preparing a crate for crates.io"
  default_prompt: "Use $publish when preparing a crate for crates.io: release gates and a publish dry-run, never publishing."
policy:
  allow_implicit_invocation: false
