interface:
  display_name: "Publish"
  short_description: "Preparing a crate for crates.io: gates, a dry-run"
  default_prompt: "Use $publish when preparing a crate for crates.io: gates, a dry-run, never publishing."
policy:
  allow_implicit_invocation: false
