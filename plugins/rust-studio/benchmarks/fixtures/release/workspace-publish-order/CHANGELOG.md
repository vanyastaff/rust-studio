# Changelog

All notable changes to this workspace are documented here. The format follows
Keep a Changelog; the workspace is versioned as one.

## [0.2.0] - 2026-06-14

### Added
- `acme-store`: `TypedStore::open` and `TypedStore::store`.
- `acme-cli`: the `acme <dir>` binary.

### Changed
- `acme-core`: `Store` is now `Debug`.

## [0.1.0] - 2026-03-02

Initial release: `acme-core::open`, `Store`, `Config`.
